const DEBUG = true;
const PORT = 7000;
const REFRESH_INTERVAL = 30; // in seconds
const UPSTREAM_URL =
    "https://konkanrailway.com/VisualTrain/otrktp0100Table.jsp";

module.exports = {
    DEBUG,
    PORT,
    REFRESH_INTERVAL,
    UPSTREAM_URL,
};
