services:
    app:
        container_name: konkan-railway_api_v1
        image: img_konkan-railway_api_v1
        restart: on-failure:3
        build:
            context: .
            dockerfile: Dockerfile
            target: base
        ports:
            - "${OUTER_PORT}:${INNER_PORT}"
        security_opt: 
            - seccomp=chrome.json
